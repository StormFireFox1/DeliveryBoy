generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./delivery-boy.db"
}

model FeedEntry {
  id    Int @id @default(autoincrement())
  title String
  link  String
  feed  String
  date  DateTime @default(now())
}